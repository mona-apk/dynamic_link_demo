name: dynamic_link_demo
description: A new Flutter application.


publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0

  # firebase_dynamic_links
  firebase_core: ^0.5.1
  firebase_dynamic_links: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
